#include <iostream>

using namespace std;

int main()
{
	double Base, Height;
	
	cout << "Enter the base: ";
	cin >> Base;
	cout << "Enter the height: ";
	cin >> Height;
	cout << "The area of the triangle is " << 0.5 * Base * Height;
	
	return 0;
}